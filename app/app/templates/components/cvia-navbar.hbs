{{#if session.isAuthenticated }}
  {{#if user.isEmployee }}
    {{cvia-employee-navbar user=user store=store }}
  {{else}}
    {{cvia-recruiter-navbar user=user store=store }}
  {{/if}}
{{else}}
  {{cvia-empty-navbar}}
{{/if}}
